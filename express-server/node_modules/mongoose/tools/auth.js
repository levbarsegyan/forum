'use strict';
const Server = require('mongodb-topology-manager').Server;
const co = require('co');
const mongodb = require('mongodb');
co(function*() {
  var server = new Server('mongod', {
    auth: null,
    dbpath: '/data/db/27017'
  });
  yield server.purge();
  yield server.start();
  const db = yield mongodb.MongoClient.connect('mongodb:
  yield db.addUser('passwordIsTaco', 'taco', {
    roles: ['dbOwner']
  });
  console.log('done');
}).catch(error => {
  console.error(error);
  process.exit(-1);
});
